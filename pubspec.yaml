name: dartshogi
description: WIP - Provides shogi and shogi variants rules and operations including shogi move generation, read and write SFEN, USI, KIF/CSA.
repository: https://github.com/WandererXII/dartshogi
version: 0.1.0
platforms:
  android:
  ios:
  linux:
  macos:
  windows:
funding:
  - https://lishogi.org/patron

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  fast_immutable_collections: ^11.0.0
  meta: ^1.8.0
  result_dart: ^2.2.0

dev_dependencies:
  benchmark: ^0.3.0
  lint: ^2.6.1
  test: ^1.25.0
